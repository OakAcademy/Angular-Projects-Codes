import { ForEmailDirective } from './for-email.directive';

describe('ForEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ForEmailDirective();
    expect(directive).toBeTruthy();
  });
});
